DATABASE_URL=postgresql://postgres:somepassword@localhost:5432/vacay_db


# The name of the header and cookie which contain JWT details
JWT_HEADER_NAME=Authorization
COOKIE_NAME=vacay_auth_token

# OAuth Provider Endpoints (OIDC)
AUTH_ENDPOINT=http://your-idp:8080/realms/yourrealm/protocol/openid-connect/auth
TOKEN_ENDPOINT=http://your-idp:8080/realms/yourrealm/protocol/openid-connect/token
JWKS_URL=http://your-idp:8080/realms/yourrealm/protocol/openid-connect/certs

# Application Configuration
CLIENT_ID=your-client-id
CLIENT_SECRET=your-client-secret
OAUTH_REDIR_URI=/auth/callback

# OAuth Parameters
OAUTH_STATE=change-me
OAUTH_SCOPE=openid
OAUTH_RESP_TYPE=code

# Frontend redirect after login
CUR_HOSTNAME=http://localhost:8001
FRONTEND_URL=http://localhost:3001/dashboard
PORT=8001

# MCP Server OAuth Configuration (required for authentication)
OAUTH_ISSUER=http://your-idp:8080/realms/yourrealm
OAUTH_AUDIENCE=your-client-audience
OAUTH_RESOURCE_URL=http://localhost:8000
